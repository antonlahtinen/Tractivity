import Foundation
import SwiftUI

struct Theme {
    static let primaryColor = Color.blue
    static let accentColor = Color.orange
    static let headingFont = Font.largeTitle.weight(.bold)
    static let bodyFont = Font.body
    static let subheadingFont = Font.title3
}
